import React from "react";
import s from "./Friend.module.css";

const Friend = (props) => {
  // return <div className={s.friend_avatar}>{props.name}</div>;
  return <div className={s.friend_avatar}>A</div>;
};

export default Friend;
